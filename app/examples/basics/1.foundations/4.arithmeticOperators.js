/* Loading a CSS file into the notebook. */
import("../../style.css");
/* js */
import { log, logTitle } from "./logger";
/* your imports */
logTitle("Arithmetic Opertators");
/*coding examples*/

// + - / % *

var resultOfAddition = 2 + 2;

var subtraction = 3 - 90;

var division = 10 / 5;

var multiplication = 3 * 30;

var remainder = 10 % 4;

log(resultOfAddition);
log(subtraction);
log(division);
log(multiplication);
log(remainder);
